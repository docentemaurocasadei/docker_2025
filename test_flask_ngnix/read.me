docker compose up --build
Poi vai su:

Prova a visitare questi URL nel browser:

http://localhost:8080/api/hello

http://localhost:8080/static/logo.png
 (se esiste il file)

http://localhost:8080/
 (dovresti vedere il messaggio "Nginx is running...")